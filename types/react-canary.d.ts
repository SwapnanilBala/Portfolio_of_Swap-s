// `ViewTransition` is a canary React API. The App Router runs a canary build
// of React internally, so the component exists at runtime; this reference
// brings in its types without an `as` cast or a suppression comment.
/// <reference types="react/canary" />
